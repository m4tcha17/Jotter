# docs/

- `architecture.md` is the load-bearing spec — read it before structural changes, and update it in the same change if you alter the data model, navigation structure, or directory layout.
- `current-task.md` tracks what's actually built vs. planned — keep its per-screen file-path references accurate when a screen moves or is renamed.
